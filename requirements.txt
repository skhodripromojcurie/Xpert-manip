# requirements.txt — Xpermanip Content Engine
#
# Installation :
#   python -m venv .venv
#   source .venv/bin/activate        # Linux/Mac
#   .venv\Scripts\activate           # Windows
#   pip install -r requirements.txt

# --- API Gemini (génération IA des fiches) ---
google-genai>=1.0.0         # SDK Python — client Google Gemini API (gratuit)

# --- Parseurs de documents ---
pdfplumber>=0.10.0         # Extraction texte PDF (meilleure gestion des layouts)
python-docx>=1.1.0         # Lecture fichiers Word .docx
python-pptx>=0.6.23        # Lecture fichiers PowerPoint .pptx

# --- Export Notion (optionnel) ---
notion-client>=2.2.1       # Client API Notion (requis seulement avec --export-notion)

# --- Utilitaires standard (inclus dans Python 3.10+) ---
# re, pathlib, json, argparse, os, datetime — stdlib, pas besoin d'installer
